package report.umwoosung.r0009;
/*ArrayList<ket,value>();--	 순차적으로 배열을 넣는것.
 * HashMap<key,value>(); --내가 원하는대로 배열을 넣는것.
 * ArrayList<,>안에 ArrayList<HashMap<key,value>>();삽입가능함
 * 
 * 
 * put (key,value)--값을 강제로 넣는것
 * >>key값은 상속되지만 value값은 상속되지않는다.
 * 
 * get(key,value)--System.out.print()에서 강제로 가져오는것.
 * size --length와 마찬가지로 HashMap과 ArrayList 에서 최대범위를 설정한다.
 * 
 * toString -- Object 에 존제함.(하는 역활이뭐지?)
 * 
 * 
 * 
 */