jsp태그
1.<%@ %>jsp 지시부.(디렉티브쪽)-속성들
Import해주는 파일들
2<%! %> 함수선언부(함수사용할때외에는 쓰지맙시다.)
선언부에 변수를 선언한다는건 멤버변수를 선언한다는것이다.

 자바스크립트에서 자바로 변환될때 별도의 함수를 만든다.
3<% %>스크립트릿


버퍼.
1만개의 파일을 보내야할때 버퍼설정을 100개를 해준다면
100개찍고 보내주고 100개찍고 보내주고 이걸 1만개할때까지 한다.
{<!-- $를 document.getElement라고 생각해야한다. 
그리고 $모양이면 무조건 jquery이다. button이라고 되있는 타입은 무조건 이걸 실행시킨다.
    		태그네임 아이디 등등 뭐든지 다가져올수있따. 이걸 셀렉터라고한다.-->
    		$() 셀렉터 html에서의 가지고싶은 객체를 안에다 써주면 찾을수있따.
$(bottun)
$(div.first)div. 의 first를 찾아온다.