JSTL((JSP Standard Tag Library)
라이브러리(library)는 여러 프로그램이 공통으로 사용하는 코드를 모아 놓은 코드의 집합이다.
결국 JSTL이란 자주 사용 될 수 있는 커스텀 태그들을 모아서 표준으로 모아놓은 태그 라이브러리다.

3차원 배열.

페이징
1.전체 데이터를 계싼하는 쿼리가 필요하다.
-select count(1) form board_info; <테이블의 있는 전체 갯수 가져오는것.
 %몇페이지가 나올수있는지 계산한다. 100만1개의 게시글이라면 10개씩 페이징할경우 10만 1개의 페이지가 나온다고 계산한다.
   블락은 몇개가 있어야하느냐? 1만 1개가 있어야한다.
   (블락의 제일 처음에서 그전으로 갈수없으니 그전것을 block처리해놓는다(현제 페이지도 마찬가지.)
   
   페이징이 화면단에서(게시판)에서 한번 일어나야하고 블락에서 한번더 일어나야한다.
   어떤걸 누르더라도 1~10까지 11을누르면 11~20까지 나오게 해야한다.
   페이징로직이 2개있다는것.
   select *from board_info
   limit 0,10;<0번쨰부터 10개를 꺼내온다. 11~20까진 limit 10,10; 20~30은 limit 20,10; 
   결국 limit 시작번호,나열할갯수;이다.
   (배열이 0부터 시작하기때문에 0부터 넣는다고 봐야한다.)
   
   페이지 나오는 SQL(블럭SQL)을 짜는법 (안된대)
  	 게시판 하나 이동할때 limit ((1-1)*10),10을해야 각각의 블락들이 제대로된 페이징을 할수있따. 
  	 다만 mysql에선 연산 안먹힌단다.
   	set @snum = ((1-1)*10);
   
   반듯이 페이징을 할때는 
   select *from board_info
   order by binum<<을 꼭해줘야한다.(order by binum desc역순.)
   limit 0,10;
   뭘로 출력하고싶은지 알아야한다는것.

	dcl에선 펑션과 프로시져가있는데 프로시져는 리턴이 있어되고 없어도된다.
	리턴을 넘기고싶다면 파라메터에 리턴타입을 적어놓는다.<<(IN loop_cnt int (1))에서 IN이라는것.
중가로가 없고, gegin과 end가있다(시작점과 끝점.)
변수선언가능(declare i int default 0;)(declare i =i변수선언 ,default 0 i=0이라고 선언)

Statr Transection  = setAutoCommit(false);

while()Do 

END while while 시작과끝.

페이징
101개가 나왔을떄 10으로 나눴다면 10.1개가 나오니 올림을 해야한다.(0.1부터 0.9까진 반올림을 해야한다.)
혹은 


