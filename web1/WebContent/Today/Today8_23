form
1.이동방법.
	`submit
	`button태그
2.특징
	`action태그에있는 url로 통째로 이동한다.
	`post방식으로 안써주면 get방식~
	
	innerHTML
	그려놓고 파씽~그려놓고 파씽이래
	
	insertAdjacentHTML
	1.넣는부분이 4개다.
		beforebegin
		aftergegin
		geforeend
		afterend
		
		static 과 interface
		
interface함수들의  default값은 public 이다.

maven jar파일 d드라이브 하드코딩경로 하는법.

main/java->com.iot.sp    -여긴 자바파일만
main/resources->db		-여긴 css,jsp
위 두개가 
classes -com.iot.sp+db

mybatice(마이바티스?)
프로퍼티스
${}jsp의 표현식.

constructor  생성자.
argement  파라메터.

스프링에서 중요한것.
ioc 
di
aop

autowired  자동연결.