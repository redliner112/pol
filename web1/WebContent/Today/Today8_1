구조체.
1.변수 테이터타입 선언하기 귀찮앙
2.그래서 배열변수가 등장.100개의변수를 같이쓸수있다.(근데 타입이 같아야된다.)
3.그래서 내가구조체가 나왔따(타입이 여러개라도 쓸수있다!)

.reaLine() 그라인을 그대로 읽고 있으면true후 리턴한후 다음줄을 읽는다. false가 날때까지.

BufferedReader br = request.getReader(); BufferedReader는 두번못읽고 한번읽고 끝나기때문에 두번읽을려면 다른것을 해줘야한다.

request.getrequestURL하게되면 내가 이동한 주소가 나오게된다.
nowUrl 현제 url
.parent() 그태그를 감싸고있는 태그를 찾음.